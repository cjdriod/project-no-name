---
import type { CollectionEntry } from 'astro:content';
import CvSection from './CvSection.astro';

interface Props {
  items: CollectionEntry<'education'>[];
}

const { items } = Astro.props;
---

<CvSection title="Education" id="education">
  <ul class="flex flex-col gap-6 m-0 p-0 list-none" role="list">
    {items.map((item) => {
      const { school, course, start, end, achievements } = item.data;
      return (
        <li class="break-inside-avoid">
          <div class="flex flex-wrap items-baseline justify-between gap-2">
            <h3 class="m-0 text-lg font-semibold text-text">{school}</h3>
            <span class="font-mono text-sm whitespace-nowrap text-faint">{start} — {end}</span>
          </div>
          <p class="mt-1 font-medium text-accent">{course}</p>
          {achievements && achievements.length > 0 && (
            <ul class="mt-2 flex flex-col gap-1 list-disc pl-6">
              {achievements.map((achievement) =>
                <li class="list-item">{achievement}</li>)}
            </ul>
          )}
        </li>
      );
    })}
  </ul>
</CvSection>
